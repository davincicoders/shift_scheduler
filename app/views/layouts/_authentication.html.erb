<% if current_organization %>
    Signed into <%= current_organization.name %> as <%= current_organization.email %>
<% else %>
    <%= link_to 'Signup', new_organizations_path, class: 'btn btn-primary' %>
<% end %>